#include <stdio.h>
#include <ctype.h>

void automata (void) {
FILE *archivo;
int carac;

archivo = fopen("lexemas.txt", r);
int TT[7][4] = {{0}}
int estado = 0;
while (estado)
{
	carac = getc(archivo);
	
	

}

return estado;
}
